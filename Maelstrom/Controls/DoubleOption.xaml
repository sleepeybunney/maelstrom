<UserControl x:Class="FF8Mod.Maelstrom.DoubleOption"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FF8Mod.Maelstrom"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="48" />
            <ColumnDefinition Width="240" />
            <ColumnDefinition Width="48" />
            <ColumnDefinition Width="240" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <CheckBox Grid.Column="0" Margin="4 8" IsChecked="{Binding Checked, RelativeSource={RelativeSource AncestorType=local:DoubleOption}}" />
        <Label Grid.Column="1" Content="{Binding Title, RelativeSource={RelativeSource AncestorType=local:DoubleOption}}" />
        <CheckBox Grid.Column="2" Margin="4 8" IsChecked="{Binding Checked2, RelativeSource={RelativeSource AncestorType=local:DoubleOption}}" />
        <Label Grid.Column="3" Content="{Binding Title2, RelativeSource={RelativeSource AncestorType=local:DoubleOption}}" />
    </Grid>
</UserControl>
