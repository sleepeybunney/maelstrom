<Controls:MetroWindow x:Class="FF8Mod.Maelstrom.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:p="clr-namespace:FF8Mod.Maelstrom.Properties"
        mc:Ignorable="d"
        Title="Maelstrom" Height="300" Width="550" ResizeMode="NoResize" Closing="OnClosing">
    <Window.Resources>
        <Style TargetType="{x:Type CheckBox}">
            <Setter Property="Foreground" Value="White" />
        </Style>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="26" />
            <RowDefinition />
            <RowDefinition Height="26" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="90" />
            <ColumnDefinition />
            <ColumnDefinition Width="90" />
        </Grid.ColumnDefinitions>
        <Label x:Name="gameLocLabel" Content="Game Location" HorizontalAlignment="Right" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" />
        <TextBox x:Name="gameLocText" TextWrapping="NoWrap" Margin="10,0,10,0" Grid.Row="0" Grid.Column="1" Text="{Binding Source={x:Static p:Settings.Default}, Path=GameLocation, Mode=TwoWay}" />
        <Button x:Name="gameLocButton" Content="Browse..." Click="OnBrowse" Margin="0,0,10,0" Grid.Row="0" Grid.Column="2" />

        <Label x:Name="optionLabel" Content="Options" HorizontalAlignment="Right" VerticalAlignment="Center" Grid.Row="1" Grid.Column="0" />
        <GroupBox Margin="10" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2">
            <StackPanel Orientation="Horizontal">
                <StackPanel>
                    <CheckBox x:Name="optionBossShuffle" Content="Shuffle Bosses" Margin="10" HorizontalAlignment="Left" IsChecked="{Binding BossShuffle, Mode=TwoWay, Source={x:Static p:Settings.Default}}" />
                    <CheckBox x:Name="optionBossRebalance" Content="Rebalance Bosses" Margin="10" HorizontalAlignment="Left"  IsChecked="{Binding BossRebalance, Mode=TwoWay, Source={x:Static p:Settings.Default}}" />
                    <CheckBox x:Name="optionStorySkip" Content="Skip Story" Margin="10" HorizontalAlignment="Left" IsChecked="{Binding StorySkip, Mode=TwoWay, Source={x:Static p:Settings.Default}}" />
                    <CheckBox x:Name="optionDrawPointShuffle" Content="Random Draw Points" Margin="10" HorizontalAlignment="Left" IsChecked="{Binding DrawPointShuffle, Mode=TwoWay, Source={x:Static p:Settings.Default}}" />
                </StackPanel>
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox x:Name="optionSeedSet" Content="Fixed Seed" Margin="10" HorizontalAlignment="Left" IsChecked="{Binding SeedSet, Mode=TwoWay, Source={x:Static p:Settings.Default}}" />
                        <TextBox x:Name="optionSeedValue" Margin="6" HorizontalAlignment="Left" Width="100" Text="{Binding SeedValue, Mode=TwoWay, Source={x:Static p:Settings.Default}}" IsEnabled="{Binding SeedSet, Mode=OneWay, Source={x:Static p:Settings.Default}}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox x:Name="optionNameSet" Content="Use Preset Names" Margin="10" HorizontalAlignment="Left" IsChecked="{Binding NameSet, Mode=TwoWay, Source={x:Static p:Settings.Default}}" />
                        <Button x:Name="setNamesButton" Content="Set..." Margin="6" Click="OnSetNames" />
                    </StackPanel>
                    <CheckBox x:Name="optionShopShuffle" Content="Random Shops" Margin="10" HorizontalAlignment="Left" IsChecked="{Binding ShopShuffle, Mode=TwoWay, Source={x:Static p:Settings.Default}}" />
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <Controls:TransitioningContentControl x:Name="goContent" Transition="Left" Grid.Row="2" Grid.Column="2" />
    </Grid>
</Controls:MetroWindow>
